@import '../../variables.styl'

.TextField
  flex-grow 3
  padding 0 10px
  background rgba(0,0,0,.7)
  color $white
  border none
  font-family 'Open Sans', sans-serif
  font-size 18px

  &:focus-visible
    outline none
    